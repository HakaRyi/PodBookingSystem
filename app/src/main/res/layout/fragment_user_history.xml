<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9F9"
    tools:context=".fragments.user_fragments.UserHistoryFragment">

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tvUserHistoryTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Lịch sử đặt phòng của bạn"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingVertical="16dp"
        android:textColor="#1A5276" />

    <!-- Ô tìm kiếm -->
    <EditText
        android:id="@+id/etUserHistorySearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Tìm theo tên phòng..."
        android:padding="10dp"
        android:layout_margin="10dp"
        android:drawableStart="@android:drawable/ic_menu_search"
        android:background="@drawable/bg_search_box"
        android:textSize="14sp" />

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvUserBookingHistory"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false" />
</LinearLayout>
